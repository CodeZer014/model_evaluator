# Core (required for scikit-learn/.joblib/.pkl models -- covers this project's sned_model_v*.joblib)
# Pinned to match the environment the .joblib files in this project were trained with --
# scikit-learn pickles are NOT guaranteed compatible across scikit-learn versions
# (internal modules like `_loss` can move/rename), so an unpinned install can break
# unpickling even though the code itself hasn't changed.
joblib==1.5.3
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.8.0
matplotlib==3.10.9

# Needed to LOAD a model that was converted with make_standalone.py (its pickle
# calls back into cloudpickle's own reconstruction helpers) -- not needed for
# a plain, not-yet-converted .joblib/.pkl file.
cloudpickle==3.1.2

# Web upload UI (evaluate_app.py)
streamlit==1.64.0

# Optional, only needed if you evaluate these model formats:
# tensorflow      # .h5 / .keras / .tflite
# torch           # .pt / .pth (full pickled model, not a bare state_dict)
# onnxruntime     # .onnx
